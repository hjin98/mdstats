# Sampling Specifications

- `shared_sampling_primitives_spec.{md,pdf}` defines the implemented
  MLFF-DATA1 source-independent autocorrelation, complete-frame block,
  deterministic assignment, and purge contracts.
