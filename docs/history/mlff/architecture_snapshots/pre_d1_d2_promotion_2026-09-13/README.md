# Pre-D1/D2-promotion MLFF mixed architecture snapshot

This directory preserves the MLFF architecture representation that was current immediately before the 2026-09-13 promotion of reconstructed D1/D2 authority.

Binding:

- exact retired documentation snapshot / transition parent: `cab2d4942042e0452df19d5a62f890db108ff49f`;
- semantic reconstruction baseline/current mdstats snapshot: `9fd82b0ed40990d56716a393aa3f7db0a2ff44d0`;
- snapshot/review date: `2026-09-13`.

The copied chapter sources, assembled Markdown, publication PDF, PDF manifest, dependency graph, publication configuration, and assembler are stored using their exact pre-promotion Git blob identities. The archive is therefore both a provenance record and an information-completeness backstop for the mixed pre-SSDP representation.

This directory is historical only. After promotion, current authority is indexed by `docs/INDEX.md` and follows `D1 -> D2 -> D3 -> D4`. No archived statement overrides its current owner merely because the older mixed manual stated it in more detail.
