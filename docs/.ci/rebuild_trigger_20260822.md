# Documentation build trigger

This file exists only to trigger the reproducible documentation CI pipeline after toolchain hardening.

Expected validation path:

`mdstats docs -> GitHub Actions -> pinned pandoc/typst -> PDF artifact -> provenance artifact`
