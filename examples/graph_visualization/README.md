# Graph Visualization Examples

Each subdirectory contains generated outputs and, where applicable, the script that reproduces them. Validation summaries have been moved to `audits/plotting/` so examples contain only user-facing examples and generation code.
