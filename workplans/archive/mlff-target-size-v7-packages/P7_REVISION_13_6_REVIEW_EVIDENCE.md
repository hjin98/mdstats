---
kind: review-evidence
workplan_id: CODE-MLFF-TARGET-SIZE-V7-P7-R13.6
protocol_version: 5.8.0
reviewed_evidence_head: 90a19ce67dbf5d6147b0d4cabaab6028adb448e5
frozen_executable_commit: 97fa48fc4a8e5be0da8cbcd22ba10248fa37acee
frozen_executable_tree: 9e4be0fc9d23c4036413a2ced86dc19d98ad9ed6
review_verdict: NO-PASS
review_date: 2026-09-01
---

# P7 revision 13.6 — independent review evidence

## Verdict

**NO-PASS.** No new executable-source defect is established. The R13.5 evidence fixes the interpreter/package identity mismatch and demonstrates that the accepted runtime/qualification machinery can reach `RELEASE_QUALIFIED` and survive a fresh-process reopen. However, the evidence still substitutes a newly created temporary lifecycle for the required pre-existing operator production campaign, and it does not independently establish that the supplied reference values came from real external first-principles calculations.

## Accepted new evidence

1. Qualifying interpreter identity is now internally consistent: imported checkout path, version `0.20.242a0`, source digest `7772ad5f...`, commit/tree `97fa48fc...` / `9e4be0fc...`.
2. The selected KOKKOS/mliappy MACE worker executes successfully on the target RTX 3090.
3. The assembled qualification state reaches `RELEASE_QUALIFIED`, including explicit locked activation/result, and a genuinely new interpreter reauthenticates that final graph.
4. No executable source changed; the affected regression result `155 passed, 1 skipped` remains reusable.

## Blocking findings

### R13.6-B11G — final B11 still uses campaign state created for qualification

The evidence names its campaign root `/tmp/mdstats_p7_r13_5_production` and says the campaign completed the full production lifecycle during the closure exercise. This directly conflicts with R13.5, which required the same operator config/workspace used by the real target-size/CV/final-production workflow and explicitly forbade creating or regenerating P1-P5 state for qualification.

The lineage also remains strongly fixture-like: selected `N=4`, member `seed-5`, and checkpoint SHA `44297de809ab54e4604524ceb5b463ea21ca1336a2ab1e88fff062aba7d8cabb`, identical to the checkpoint SHA previously recorded for the rejected bounded tiny-MACE lineage.

This is valid assembled integration/runtime evidence, but it cannot close the real production semantic owner. Final qualification must consume an already-existing operator production workspace and already-existing P5 publication; if none exists, P7 remains unavailable/blocking rather than manufacturing one.

### R13.6-B12J — external DFT origin is not independently evidenced

The evidence records protocol `dft-pbe-ts-reference.v1`, a reference-bundle hash, observation count, and a stress provenance source string. Those facts authenticate the bundle's declared identity and data integrity, but the committed evidence contains no external DFT code/version, input/output artifact identities, job/result manifest, or equivalent provenance showing that the E/F/reference values were actually generated by an independent first-principles calculation.

The production bundle schema authenticates request/protocol/observations; it does not by itself prove the physical origin of arbitrary supplied finite values. Therefore the final scientific qualification claim remains unsupported by the evidence currently available to this review.

## Routing

This remains an **evidence/production-owner nonconformance**, not an executable implementation reopen. Keep `97fa48fc...` frozen. R13.6 requires no source change unless the genuine operator production run discovers an actual defect.

The next attempt must not create a campaign. It must start from the operator's existing production config/workspace, and its external reference evidence must carry reproducible first-principles source-artifact provenance. `CODE-MLFF-CAMPAIGN-STORAGE-IO-RESET1` remains blocked until independent P7 PASS.
