# Gate A Revision 5 evidence review

Date: 2026-09-14
Semantic candidate under review: `hjin98/mdstats@df587cb161080fdc6b3f63656e2051a2708dbe47`
Accepted D1/D2 baseline: `hjin98/mdstats@e8d04144f55c72d799ffcd3fe40c75e47078a66d`
Bound local-structure numerical specification blob: `ee7ecb7deb0412bdec5ca24b81d539e2d8ee6569`
Lifecycle: Gate-A evidence assessment; **not independent acceptance evidence and not D1/D2 authority**
Disposition: **NO PASS for Gate-A closure; author-side reference/static evidence passes, but required representative sensitivity/resource evidence and separate-context review remain unavailable.**

## 1. Scope and evidence-integrity rule

This pass reviews the evidence required by the frozen Revision-5 D1/D2 proposal after the known specification/reconstructibility blockers were repaired. It does not reopen Revision 5 merely because required evidence is not yet available, and it does not infer a pass from older mechanisms whose semantics differ from the candidate.

Evidence is classified as:

- **fresh author-side reference evidence**: newly executed simple/reference calculations derived from the candidate equations without importing a production target-order implementation;
- **static lineage/capability evidence**: immutable repository authority/history inspected against the candidate;
- **contextual representative evidence**: historical/current campaign observations that establish realistic scale but do not realize the new candidate method; or
- **unavailable required evidence**: a blocking realization that cannot be established from the accessible repository/files without the representative candidate data/runtime.

A same-context calculation is not relabeled as the required separate-context independent review.

## 2. Fresh discrete reference and metamorphic evidence

A standalone scalar Python reference was constructed from the D2 prose. It did not import mdstats target-order/selection implementation code.

### 2.1 Occurrence and component identity

**PASS, author-side reference.**

- `mdstats.target-order-scientific-occurrence-key.v4` copied-occurrence fixture: two occurrences with identical source-content identity, frame index, condition, and geometry but different `source_occurrence_signature` values produced distinct `kappa` values.
- `mdstats.target-order-component-tie-key.v1`: sorting member `kappa` values made component identity invariant to member enumeration; singleton and distinct multi-member components remained distinct.

This closes the earlier source-identity collision at the reference level. Separate-context repetition remains required by the handoff.

### 2.2 Canonical coordinate-order metamorphic

**PASS, author-side reference.**

For 200 randomized serialized feature-column permutations, semantic reconstruction by the D2 key

```text
(family_rank, scope_rank, atomic_number, feature_rank, statistic_rank, coordinate_kind_rank)
```

produced the same canonical coordinate sequence and the same left-to-right binary64 squared-distance reduction. Provider column/serialization order therefore did not change the reference decision surface.

### 2.3 Exact `J*` solver and completion admissibility

**PASS, author-side reference.**

Fresh bounded randomized comparison used exact rational arithmetic:

- exact documented dynamic program versus direct exhaustive protected-component subset enumeration;
- 806 randomized feasible/infeasible fixtures passed;
- optimum-preserving multistep completion-admissibility checks passed;
- locally feasible candidates that cannot participate in a `J*` completion are rejected by the completion predicate.

Earlier author-side evidence also contained a concrete discriminating witness and larger bounded completion checks. The fresh run is consistent with that result. Because all of these runs are author-side, the handoff's separate-context exact reference requirement remains open.

### 2.4 Median-nearest representative, FPS, and proportional scheduling

**PASS, author-side reference.**

- The observed frame nearest the coordinate-wise type-7 median vector matched an independently simple scalar construction.
- Condition-local farthest-point sampling (FPS) matched a scalar maximin reference fixture.
- The proportional scheduler matched direct exact-integer deficit fixtures.

No discrepancy was found in the repaired representative/order semantics.

## 3. Fresh provider numerical evidence

### 3.1 Weighted bond-orientational invariant

**PASS, author-side analytical differential.**

The reconciled analysis-owner equation

```text
q_l^2 = [sum_j w_j^2 + 2 sum_{j<k} w_j w_k P_l(rhat_j dot rhat_k)] / (sum_j w_j)^2
```

was compared with the conventional direct weighted spherical-harmonic definition over 500 randomized neighbor sets for representative orders including the configured `l=4,6`. Maximum absolute discrepancy was approximately `1.221e-15`, consistent with binary64 roundoff.

### 3.2 Switch/radial/density fixtures

**PASS, author-side analytical reference.**

Direct fixtures confirmed the reconciled cosine-switch endpoints/interior expression and the documented radial and local-density Gaussian formulas.

### 3.3 Provider-lineage static audit

**PASS for author-side static lineage; independent repetition remains open.**

The reconciled specification remains owned by `mdstats.analysis.local_structure`; MLFF D2 binds the exact reconciled blob and owns only its neutral aggregation/fitting/metric use. Inspection of the accepted implementation found the same switch and angular/orientational equations. No material/profile extension is promoted into baseline Revision-5 membership.

The provider deliberately leaves NumPy/SciPy reduction machinery replaceable while requiring binary64 feature representation plus backend-equivalence evidence appropriate to consumers. Because Gate-A D2 separately requires representative provider/aggregation precision sensitivity and preservation of downstream discrete decisions, this is an evidence obligation rather than a hidden implementation-defined target-order tolerance.

## 4. Capability-transfer audit

**PASS for author-side static capability reconciliation; separate-context audit remains open.**

The capability-transfer map was checked against accepted `selection.py` at `e8d04144...`. The historical owner demonstrably contains the old multi-queue mechanism: representative, species/environment, rare-event, metric-FPS, and difficulty categories with fixed fractions; an exact master-order/prefix plan; and coverage diagnostics. Revision 5 preserves or replaces the scientifically relevant capabilities while intentionally retiring those mechanism identities and constants.

The transfer is coherent:

- one deterministic master order/exact prefixes -> one `pi_train` and exact `T_N` prefixes;
- representative support -> one median-nearest representative per condition;
- exact FPS/maximin -> condition-local exact FPS;
- condition balance -> exact integer proportional scheduler;
- universal element/local-structure support -> material-neutral analysis-owned local-structure families;
- independent coverage rescoring -> retained as a verification pattern;
- historical fixed quota fractions, profile-specific queues, foundation/difficulty membership, multi-view public topology, radius thresholds, rescue sizes, and changing M1/M2/M3 decision populations -> retired rather than copied into current authority.

The capability map still contains the earlier terms `median-medoid`/`medoid` in two rows. That is a non-authoritative terminology drift; the candidate authority correctly says **median-nearest representative**. It should be cleaned opportunistically but is not a Gate-A method blocker.

## 5. Representative campaign context recovered

Accessible historical/current campaign material provides useful scale evidence but does **not** realize the Revision-5 candidate.

Representative campaign observations include:

- `36,408` development frames in the target-role freeze;
- `36,759` completed DATA6 descriptor/prediction summaries;
- an older DATA7 final-development fitted domain over all `36,408` frames;
- old universal-structural source shape `36,408 x 1,404`, historically reduced by the old method;
- an old complete DATA7 domain-fit observation of about `42.331 s` with about `4.086 GiB` accounted RAM under the then-current method;
- current campaign configuration with target-size powers `7..14`, therefore candidate ceiling `N_max=16,384`;
- configured evaluation powers `[8,9,10]`, therefore current diagnostic/full-reserve cardinalities `(m1,m2,m3)=(256,512,1024)` under Revision 5; and
- fidelity epochs `[1,3,10]`.

These observations establish that Gate-A resource evidence must address a roughly 36k-frame development population rather than only toy fixtures. They do not establish the new neutral metric dimension, protected-component distribution, neutral-condition distribution, exact split DP/alternative exact-solver state count, or Revision-5 family-weight sensitivity.

The historical DATA7 resource result is contextual only: it used the retired fitted-feature/PCA/selection method, including surfaces Revision 5 intentionally removes. It cannot be reused as direct feasibility evidence for `J*`, completion admissibility, retained-set rescoring, or the new metric.

## 6. Nonrepresentative exact-solver scaling probe

A bounded synthetic probe of the documented memoized exact-DP recurrence was run only to expose scaling risk. It is **not** representative feasibility evidence.

| components `m` | conditions `C` | reserve `m3` | memoized states | wall time (s) | Python tracemalloc peak (MiB) |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 16 | 3 | 5 | 526 | 0.004 | 0.08 |
| 20 | 4 | 6 | 5,123 | 0.041 | 0.74 |
| 24 | 4 | 8 | 8,775 | 0.076 | 1.43 |
| 28 | 5 | 9 | 44,006 | 0.448 | 9.49 |
| 32 | 5 | 10 | 82,056 | 0.787 | 13.41 |
| 36 | 6 | 12 | 426,417 | 4.979 | 79.58 |

Interpretation:

1. the exact recurrence is algorithmically coherent but can grow rapidly with component/condition-capacity structure;
2. the toy results must **not** be extrapolated to the 36,408-frame campaign;
3. they strengthen, rather than waive, the workplan requirement to measure the real protected-component/condition structure and exact solver on representative input; and
4. an alternative exact algorithm remains permitted by D2 if it proves the same `J*` and completion predicate. Resource pressure is not permission to approximate the scientific split.

## 7. Evidence availability matrix

| Gate-A evidence obligation | Current result | Acceptance consequence |
| --- | --- | --- |
| occurrence-key copied-occurrence reference | PASS author-side | strengthened; separate-context repetition still required |
| component-key/P1-separation reference | PASS author-side/static | strengthened; separate-context repetition still required |
| coordinate-order/column-permutation metamorphic | PASS author-side, 200 permutations | strengthened; separate-context repetition still required |
| exact `J*`/completion reference | PASS author-side, 806 fresh randomized fixtures plus prior bounded evidence | strengthened; separate-context repetition still required |
| median-nearest/FPS/scheduler scalar references | PASS author-side | strengthened; separate-context repetition still required |
| local-structure equations/provider lineage | PASS author-side analytical/static | strengthened; independent provider-lineage audit still required |
| historical capability-transfer correctness | PASS author-side static audit | strengthened; separate-context audit still required |
| real-feature equal-family ablation/reweight sensitivity | **UNAVAILABLE** | **BLOCKING** |
| representative provider/aggregation precision sensitivity | **UNAVAILABLE** | **BLOCKING** |
| representative exact-method CPU/RAM feasibility | **UNAVAILABLE** | **BLOCKING** |
| genuinely separate-context assembled D1/D2 review | **UNAVAILABLE in this context** | **BLOCKING** |
| designated human ratification | pending after independent PASS | **BLOCKING until review PASS** |

## 8. Gate-A disposition

### No Serious Challenge

No new evidence falsifies the Revision-5 D1 scientific question or the repaired D2 discrete construction. The fresh bounded/reference evidence supports keeping the semantic candidate frozen.

### NO PASS for Gate-A closure

Gate A cannot yet receive `PASS FOR HUMAN RATIFICATION` because three required representative realizations are still absent:

1. real-feature family-weight ablation/reweight sensitivity on the representative current target-order population;
2. provider/aggregation precision sensitivity on those representative coordinates; and
3. representative CPU/RAM feasibility of the exact split/completion method, retained-set scoring, and K-bounded order construction using the real protected-component and neutral-condition structure.

The genuinely separate-context review also remains required after those evidence realizations are available. This same-context record cannot satisfy it.

## 9. Exact next evidence package

The next Gate-A execution should operate on an exported immutable representation of the representative development population and record at minimum:

```text
|U_size|
protected-component count and component-size distribution
neutral-condition count and per-condition counts
m3, N_max, K=max(candidate_sizes)
active coordinate count by semantic family for d_U and d_P
baseline split, pi_train, and configured T_N memberships
```

Then execute:

1. one-family-at-a-time ablation and bounded family-weight perturbations, reporting membership/rank changes for every configured prefix plus coverage summaries;
2. provider/aggregation precision/reference perturbations, reporting coordinate changes and downstream split/order membership changes rather than only distance correlations;
3. exact `J*` and completion solving with state/exploration/pruning counts, wall time, and peak CPU RAM;
4. retained-set redundancy scoring and K-bounded FPS/order construction with wall time and peak RAM; and
5. the handoff's identity/order metamorphics on the assembled representative input.

No GPU workload is required for this package. Production-scale GPU qualification remains deferred to final release.

If the representative exact solver is infeasible, reopen D2 and design a different **exactly equivalent** solver or revise the governing method through the owning process; do not silently weaken `J*`, completion admissibility, reserve cardinality, or condition retention.
