---
kind: independent-review-record
protocol_version: 6.4.0
status: PASS
lifecycle: PROPOSED_REVIEW_PASS_AWAITING_STAKEHOLDER_RATIFICATION
workplan_id: MLFF-D1-D2-SSDP64-AXIOMATIC-FORMALIZATION-1
accepted_basis: cb07d68372f1b6d25f9e8b62a2fb7e31fb82e824
immutable_candidate_target: e827aef9bdceb97aae5be6e89de0585a95dcf71c
binding_handoff: 27155c0fc55bb281cb05885ca20831ba6dd7d976
prior_candidate_target: 12af89b860511277246e853a1e7ba22b86cec39f
prior_review_result: R6_NO_PASS
serious_challenge: NONE
---

# Independent Review R7 — MLFF D1/D2 Protocol-6.4 formalization

## 1. Verdict

**PASS.**

No Serious Challenge is raised against accepted D1/D2 authority at
`cb07d68372f1b6d25f9e8b62a2fb7e31fb82e824`.

Fresh assembled-candidate review of immutable target
`e827aef9bdceb97aae5be6e89de0585a95dcf71c` finds:

- proposed D1 scientific meaning losslessly equivalent to accepted D1;
- proposed D2 numerical meaning losslessly equivalent to accepted D2;
- direct semantic dependency representation complete for the reviewed bounded
  106-object surface;
- no unresolved prerequisite, cycle or D1->D2 authority inversion;
- renderer repair algebraically equivalent and renderer-safe;
- weighted-quantile D4 owner conformant to D2.DEF.014 with current applicable
  real-owner evidence;
- PEM/HAS state coherent and no high-impact unresolved notice.

The candidate is therefore review-passed but remains **proposed**. Stakeholder
ratification of this exact reviewed target is still required before canonical
promotion/reconciliation.

## 2. Review basis and independence

The review reconstructed accepted meaning directly from the four canonical
papers at accepted basis
`cb07d68372f1b6d25f9e8b62a2fb7e31fb82e824`:

- `docs/methods/mlff_scientific_method.md`;
- `docs/methods/mlff_target_training_order_scientific_method.md`;
- `docs/methods/mlff_numerical_algorithmic_method.md`;
- `docs/methods/mlff_target_training_order_numerical_algorithmic_method.md`.

The R7 handoff, R7 repair closure, prior reviews, CI runs and PR comments were
treated only as evidence/challenge material.

At review time:

- accepted `main` still resolved exactly to the accepted basis;
- PR #16 remained open/draft and bound to the R7 handoff descendant;
- D1 candidate blob was
  `dc05bc0a8fe5790b6ffe91bfacbd2bb25360a498`;
- D2 candidate blob was
  `74c0c0588617b7f48fd93bc21e534f538f037ef8`;
- R7 dependency trace blob was
  `1c76b77e8960e1fefe5b8ec371be8d7e17827b00`.

## 3. D1 substantive review — PASS

Fresh D1 reconstruction found no scientific drift in:

- atomic/source occurrence, geometry and canonical-label identity;
- label-compatibility and energy-conserving E/F/stress conventions;
- protected-relation closure and evidence-role noninterference;
- correlation diagnostic interpretation;
- exact `U_size = P_train dot-union M3` target-size experiment;
- candidate-size/evaluation/fidelity/seed policy family;
- controlled-variable semantics;
- deterministic nested evaluation ladder;
- force-RMSE response and practical-equivalence coordinate;
- one deterministic complete `pi_train` and exact nested `T_N`;
- selector information boundary and no-feedback requirement;
- required family/reference-measure semantics;
- fixed 0.95 coverage and 0.01/0.99 extent coordinates;
- canonical hard obligations and membership qualification;
- configured-shell repair continuity and automatic-screen admission;
- recommendation/operator-decision separation;
- P5 1:10:1 E/F/S objective, binary masks and no training-head scalar;
- exact foundation/head identity and composition-correction identifiability;
- true-reference replay default, pseudo explicit opt-in and replay lineage;
- common protected 256-frame monitor;
- protected post-selection folds and independent CV/production thresholds;
- fixed-budget all-position CV and fresh production;
- downstream no-feedback.

The scoped target-order D1 source remains the sole owner for membership-design
meaning where it supersedes general-paper prose.

No D1 Challenge is raised.

## 4. D2 substantive review — PASS

Fresh D2 reconstruction found no numerical-method drift in:

- source numerical conventions and binary64/exact-integer decision domains;
- finite-sequence autocorrelation estimator and complete-frame blocks;
- protected-event relation construction;
- structural target-size policy;
- exact M3 split and deterministic condition-balanced `pi_eval`;
- candidate-common P3 fitted preparation;
- required-family catalog/applicability;
- correlation-balanced witness weights normalized exactly once;
- stable weighted quantiles comparing cumulative stored mass directly to
  governed `q`;
- robust scale, leave-one-out radius and exact adjacency;
- direct MVQUAL coverage, extent predicates and canonical obligations;
- FEAS1 configured horizon;
- MVSEL2 Phase A/B semantics;
- full-forward oracle and certified-lazy exactness;
- REPAIR2 eligibility/frontier/objective/limits/rank inheritance;
- independent configured-prefix MVQUAL;
- E0 residual fit and null-space transfer;
- robust P5 objective and replay/pretraining-head E0 separation;
- P3 update normalization, force estimator, reducer, funnel and configured
  ceiling;
- continuous P3 trajectory/restart;
- replay geometry/label/qualification/exposure semantics;
- deterministic common monitor and fold construction;
- checkpoint predicates, role currentness and fresh production;
- authenticated continuation, equivalence registry and execution
  noninterference;
- fail-closed typed failure set.

No D2 Challenge is raised.

## 5. Dependency trace — PASS

### 5.1 Structural closure

The exact R7 trace has:

- 106 formal D1/D2 objects;
- 106 corresponding subject rows;
- no missing subject;
- no unresolved formal prerequisite;
- no object-level cycle;
- no D1 subject depending on a D2 concretization.

The explicit formal-ID scan leaves one deliberate textual cross-reference only:

```text
D2.DEF.029 !-> D2.DEF.020
```

This remains a correct non-edge because D2.DEF.029 mentions D2.DEF.020 solely to
state that the selector Phase-A predicate is intentionally distinct from the
MVQUAL predicate.

### 5.2 R7 repair edges

Both R7 additions are necessary direct dependencies:

```text
D1.DEF.010 -> D1.DEF.006
D2.DEF.062 -> D2.DEF.015
```

`D1.DEF.010` explicitly classifies `M_i` as P3 model-selection evidence and
not held-out/checkpoint-monitor evidence, so changing the local evidence-role
owner changes its interpretation.

`D2.DEF.015` explicitly makes non-finite robust-scale input a preparation
failure, so changing that policy directly changes the typed
“non-finite fitted statistics” member of D2.DEF.062.

### 5.3 Re-challenged non-edges

The following challenged non-edges remain valid:

- `D2.DEF.056` obtains frozen-`T_N`, fold-role and monitor-external meaning
  through direct parent `D1.DEF.024`;
- `D2.AX.005` obtains M3/monitor/replay/production-threshold semantics through
  its direct production/checkpoint parents;
- `D1.DEF.013` excludes post-selection evidence by role class and exact scoped
  import, not by consuming later realized memberships;
- `D2.DEF.016`'s two-reference family precondition does not independently add
  another D2.DEF.062 failure member because the operative leave-one-out
  denominator/reachable-mass failure is owned by D2.DEF.017;
- empty selected extent in `D2.DEF.021` is a failed qualification predicate,
  not a separate workflow typed-error member;
- `D2.AX.002` and `D2.AX.004` currentness/staleness effects remain mediated
  through their direct repair/replay/checkpoint/continuation owners;
- `D2.DEF.060A` remains a comparison-relation registry rather than a direct
  consumer of every value-generating operand;
- target-order foundation residual identity remains independently source-owned
  by exact `D2.IMP.ORDER`; post-selection composition-correction object
  `D1.DEF.020` is not silently made the target-order identity owner.

### 5.4 Apparent forward local dependencies

Five direct D1 edges point to local formal objects that occur later textually.
This does **not** create a first-use definition gap for the declared competent
reader because the exact-import registry appears first and explicitly treats
`D1.IMP.*` roots as already-defined accepted objects. The affected concepts
(`pi_train/T_N`, evaluation ladder, selector authority, membership
qualification) are already defined in those exact basis-pinned imports; the
later local definitions refine their Protocol-6.4 representation.

There is no semantic cycle. The object graph is acyclic.

### 5.5 Additional directionality challenges

Two heuristic-looking pairs are correctly one-way:

- `D1.DEF.009` introduces the policy coordinate `epsilon`; D1.DEF.011 then
  constrains its response-unit meaning and binding class. D1.DEF.011 consumes
  the policy coordinate; adding the reverse edge would incorrectly manufacture
  a cycle.
- `D2.AX.003` owns P3 continuous-trajectory/authenticated-restart semantics;
  the later general continuation object D2.DEF.060 consumes that owner. The
  reverse edge is not required.

## 6. Renderer — PASS

All four current MLFF method papers and both proposed D1/D2 candidate kernels
contain zero unsupported `\operatorname` occurrences and zero raw escaped
`\#` cardinality expressions.

The scoped target-order D2 renderer change is algebraically exact:

- `H(c)` changed from named cardinality notation to explicit
  `|{...}|`;
- nested named arithmetic means for `D(c)` changed to explicit finite sums
  divided by `|M(c)|` and `|W_m(c)|`;
- the empty-family result remains exactly `D(c)=0`.

No numerical or scientific meaning changes.

## 7. D4 real-owner evidence — PASS/current

At the immutable R7 target:

- weighted-quantile real owner blob:
  `15d20eae5a7b481d636d20fc2281c743e9abe0df`;
- focused FP64 regression blob:
  `8da42708ac601b335dfb3c9d818c302baccb1a78`;
- real-owner target-order suite blob:
  `01c6d99872c136cff91777e65ce8331807f8a016`.

Actions runs:

- `35300235175`;
- `35300268107`;

both completed successfully with the focused weighted-quantile regression and
real-owner target-order integration/regression under the supported CPU
MLFF/test environment.

Comparison from both evidence commits to R7 changes only the temporary
validation workflow plus documentation/review/workplan artifacts; the real
owner and both test blobs are byte-identical.

The owner implementation stable-sorts values, computes binary64 cumulative
stored weights, validates only finite positive terminal mass, and calls
`searchsorted(cumulative, requested, side="left")` without multiplying
`requested` by terminal cumulative mass.

Independent binary64 reconstruction reproduced all required adversaries:

```text
counts (2,6),   q=0.25: sum=1.0, terminal=1.0000000000000002, direct=0, old=1
counts (50,1),  q=0.01: sum=1.0, terminal=1.0000000000000002, direct=0, old=1
counts (1,6),   q=0.75: sum=0.9999999999999996, terminal=0.9999999999999996, direct=4, old=3
counts (1,150), q=0.99: sum=1.0, terminal=0.999999999999995, direct=148, old=147
```

The D4 repair therefore conforms to D2.DEF.014 and current evidence remains
applicable.

A repository search found no current downstream documentation/specification
copy of the old terminal-mass-rescaling rule. Remaining weighted-quantile hits
outside the current method/owner are historical or retired artifacts.

## 8. PEM / HAS — PASS

Accepted Project Engineering Memory remains blob
`67e3130d3703d9fc26ed0afe94fa2506a176e9ce` and reports no high-impact
unresolved notice.

The bounded Historical Applicability Set remains coherent:

- SP-001 APPLICABLE;
- SP-002 APPLICABLE;
- SP-003 APPLICABLE;
- SP-004 APPLICABLE;
- FF-001 APPLICABLE;
- FF-002 APPLICABLE;
- FF-003 NOT_APPLICABLE;
- FF-004 NOT_APPLICABLE;
- FF-005 APPLICABLE.

No memory evidence displaces or challenges the accepted basis.

## 9. Lifecycle disposition

Immutable R7 target

`e827aef9bdceb97aae5be6e89de0585a95dcf71c`

is **independent-review PASS**.

This review does not ratify or promote it. The candidate remains proposed and
must not replace canonical D1/D2 authority until the stakeholder explicitly
ratifies this exact reviewed target.

After ratification, canonical method-paper promotion/reconciliation and bounded
dependent documentation/semantic-history closure may proceed according to the
active workplan. No new implementation mechanism is required by this review.

GPU qualification remains deferred to final release qualification under the
standing project policy.
