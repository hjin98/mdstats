---
kind: independent-review-handoff
protocol_version: 6.4.0
status: AWAITING_FRESH_INDEPENDENT_R2_REVIEW
workplan_id: MLFF-D1-D2-SSDP64-AXIOMATIC-FORMALIZATION-1
accepted_basis: cb07d68372f1b6d25f9e8b62a2fb7e31fb82e824
immutable_candidate_target: a09e2b857da64e18fd7a6044f0fdad29aa29365d
prior_r1_target: c9d3a9b4d8a226a7298e4e6088d3db19e98c1ea2
prior_r1_result: NO-PASS
branch: design/mlff-d1-d2-ssdp64-axiomatic-formalization
---

# Independent Review R2 handoff — MLFF D1/D2 SSDP 6.4 formalization

## 1. Review target and independence requirement

Perform a fresh independent Protocol-6.4 D1/D2 review of immutable target

```text
a09e2b857da64e18fd7a6044f0fdad29aa29365d
```

against accepted mdstats basis

```text
cb07d68372f1b6d25f9e8b62a2fb7e31fb82e824
```

Do not inherit the authoring context, R2 repair closure conclusions, PR description, prior review conclusions, or implementation behavior as authority. They are evidence to challenge. Reconstruct the accepted D1/D2 meaning from the accepted basis and attempt to falsify the assembled R2 candidate itself.

The prior immutable R1 target `c9d3a9b4...` returned NO-PASS. Its review record is retained as historical review evidence; R2 is not a delta-only check and must independently review the complete new candidate.

## 2. Exact R2 candidate surface

The immutable target contains exactly these proposed/current changes relative to accepted basis:

1. `workplans/active/MLFF_D1_SSDP64_AXIOMATIC_AUTHORITY_R2_REPAIRED.md` — sole proposed R2 D1 formal kernel;
2. `workplans/active/MLFF_D2_SSDP64_AXIOMATIC_AUTHORITY_R2_REVIEW_CANDIDATE.md` — sole proposed R2 D2 formal kernel;
3. `workplans/active/MLFF_D1_D2_SSDP64_DEFINITION_DEPENDENCY_TRACE_R2_FINAL.md` — bounded non-authoritative direct-definition trace;
4. `docs/methods/mlff_target_training_order_numerical_algorithmic_method.md` — representation-only renderer correction;
5. the active workplan, R1 review record, and R2 author-repair closure as workflow/evidence artifacts.

Superseded authoring drafts were removed from the current candidate tree before target binding. Their historical commits remain recoverable but they are not part of the R2 semantic candidate.

## 3. Governing review questions

### D1 scientific-formulation review

Verify that the proposed D1 kernel is a lossless formalization of the two accepted D1 papers, with no silent strengthening, weakening, new exclusion, changed estimand, new evidence role, or changed owner boundary. In particular challenge:

- source/label/stress/strain identity and compatibility;
- protected relations and typed evidence-role permissions;
- target-size controlled-variable semantics and automatic-screen sufficiency;
- target-order family/support meaning and fixed `0.95`, `0.01/0.99` coordinates;
- qualification versus ranking separation and shell-repair meaning;
- foundation objective and composition-level E0 identifiability;
- true-reference/pseudo replay label-mode and geometry-lineage semantics;
- common target monitor, folds, role thresholds, fixed-budget CV and fresh production;
- fixed/configurable/default binding classifications;
- validity, uncertainty, typed infeasibility and downstream no-feedback boundaries.

Raise SERIOUS CHALLENGE only if the accepted D1 authority itself is materially defective; otherwise defects in the proposed formalization are ordinary R2 blockers.

### D2 numerical-method review

Verify exact numerical equivalence to accepted D2, including:

- Geyer/unbiased autocorrelation and complete-frame/event/protected-unit construction;
- five-step component ordering plus first-predecessor exact `M3` allocation;
- condition-balanced `pi_eval` and current P2 structural policy;
- P3 common preparation and no candidate-dependent refit/renormalization;
- exact family catalog and applicability;
- one-time binary64 family-weight normalization and stable weighted quantile;
- robust scale, local radius, adjacency, extent, independent MVQUAL and selector tolerances;
- obligation canonicalization, FEAS1, MVSEL2 full-forward/lazy equivalence;
- REPAIR2 frontier/objective/limits/rank inheritance/reconstruction/continuation;
- minimum-three-qualified admission and exact `q -> min(q,4) -> 2 -> 1` reducer semantics;
- selected-head E0 fit/null-space transfer;
- robust P5 dimensional Huber objective and nine-entry stress reduction;
- replay label modes, replay-first exposure, no duplication/head scalar, `drop_last=true` geometry;
- monitor sampler, fold/purge construction, role-effective checkpoint predicates;
- continuation, precision/equivalence and typed failure semantics.

Implementation may be inspected only as conformance/disambiguation evidence after the accepted numerical owner is reconstructed. A current implementation result cannot legitimize candidate drift from accepted D2.

## 4. Required R1 regression challenges

R2 must explicitly re-attempt all nine R1 failure classes:

1. **Weight normalization:** use the 37-witness/2-witness adversarial binary64 case. `Q(0.5)` must consume once-normalized stored weights and select index 36; any second normalization is a blocker.
2. **Tolerance separation:** construct coverage inside the interval distinguishing MVQUAL `+1e-12` from MVSEL2 `-1e-14`; the predicates must remain separate.
3. **P1/P2/P3 closure:** verify exact split/evaluation/structural-policy/reducer/statistical definitions are reconstructible without hidden chat or implementation reverse engineering.
4. **Family catalog:** verify exact universal/profile/pair/response/foundation families and applicability.
5. **REPAIR2:** verify replacement frontier, 2-pass/32-swap/64-shortlist limits, rank inheritance/future displacement and no-extra-shell continuation.
6. **Replay:** verify true-reference default, pseudo explicit opt-in/frozen-foundation binding, separate true-reference monitor and geometry-label separation.
7. **Traceability:** verify every specialized prerequisite resolves to an exact basis-pinned import or formal object, and no material direct dependency is missing.
8. **Parameter binding:** verify fixed coordinates cannot be misconstrued as ordinary configurable defaults and configurable role defaults are not promoted to universal constants.
9. **PEM/HAS:** verify the workplan's canonical basis/HAS remains applicable. Remote accepted `main` was unchanged at target preparation; independently recheck if review occurs after any accepted-state movement.

## 5. Protocol-6.4 definition/traceability challenge

Independently traverse definition-before-use and reverse impact. Do not treat the generated trace as proof.

Required checks:

- every material symbol/operator/relation has a source-available definition/import before substantive use;
- exact imported sections genuinely contain the semantics attributed to them;
- family/instance/default and fixed/configurable/derived classifications are coherent;
- empty-set, zero-denominator, non-finite, missing-provider, rank-deficient and infeasible regimes are defined or fail closed;
- direct `USES_DEFINITION` edges are material and complete for the declared two-file scope;
- D1 has no downward semantic dependency on D2 concretizations;
- D2 -> D1 concretization edges point to the correct owner;
- no unintended semantic strongly connected component exists;
- no claim is warranted merely by definition, citation count, implementation behavior, or this trace.

## 6. Renderer check

Verify the candidate/current MLFF authority surface contains no renderer-invalid raw `\#` cardinality or `\operatorname{...}` macro at the repaired locations. Confirm that set-cardinality and explicit-finite-mean rewrites are mathematically and numerically equivalent to accepted meaning.

## 7. PEM/HAS basis

The candidate workplan binds:

```yaml
pem_basis:
  accepted_project_state: cb07d68372f1b6d25f9e8b62a2fb7e31fb82e824
  accepted_pem: hjin98/mdstats@b5d101d8f73d3efd63ef4e70b3913e7d281406ce:PROJECT-ENGINEERING-MEMORY.md
  candidate_overlay_semantic_candidate: NONE
```

with R1 dispositions SP-001/002/003/004 and FF-001/002/005 APPLICABLE, FF-003/004 NOT_APPLICABLE. Treat PEM only as non-authoritative hypothesis/history support and independently verify current owners.

## 8. Review disposition

Return **PASS** only if the complete immutable target is lossless with respect to accepted D1/D2 and no genuinely blocking Protocol-6.4 definition/source/dependency defect remains.

Return **NO-PASS** for any semantic drift, missing material definition/import, source ambiguity, owner inversion/cycle, parameter-classification error, numerical non-equivalence, replay/exposure drift, renderer repair meaning change, or other defect that would let a descendant satisfy the proposed formal kernel while violating accepted authority.

Do not mutate the immutable target during review. If repair is required, preserve the review record, reopen the workplan, create a new candidate identity, and require another fresh review.

A PASS does not itself promote the proposed D1/D2 kernels. Stakeholder ratification of the exact reviewed target remains required before canonical promotion/reconciliation. GPU qualification remains outside this documentation-formalization review and stays deferred under the established project policy.