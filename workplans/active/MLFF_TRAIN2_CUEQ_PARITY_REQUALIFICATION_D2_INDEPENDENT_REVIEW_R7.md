---
kind: D2-independent-review
protocol_version: 6.4.0
status: PASS
workplan: workplans/active/MLFF_TRAIN2_CUEQ_PARITY_REQUALIFICATION_WORKPLAN.md
reviewed_candidate: db2ed47e8c999cb61507803610c72c0fa7ffaaf7
reviewed_candidate_blob: 7843a41172d25c231d4c589aebc0214ddec42bd1
lifecycle_head_basis: 7c94925426c117a3a26169bd5d2bcc801a2023ba
accepted_parent_d2_kernel: a759e81aa1b4c70c8fb513c569ddce57e99cbdb2
accepted_parent_d2_source: a4824d28775164aa942fd29fa97ee0957eb87e6f
serious_challenge: false
highest_blocking_owner: NONE
stage_C_state: BLOCKED_PENDING_STAKEHOLDER_INSTANCE_RATIFICATION
D3_D4_state: BLOCKED_PENDING_STAGE_C
date: 2026-09-25
---

# Candidate-10 fresh independent Protocol-6.4 D2 Review R7

## 1. Immutable binding and disposition

This Review targets immutable Candidate 10 exactly at:

db2ed47e8c999cb61507803610c72c0fa7ffaaf7

with Candidate-10 Git blob:

7843a41172d25c231d4c589aebc0214ddec42bd1.

The lifecycle descendant:

7c94925426c117a3a26169bd5d2bcc801a2023ba

is a direct one-commit descendant of Candidate 10. The C10-to-descendant diff changes only:

- workplans/active/MLFF_TRAIN2_CUEQ_PARITY_REQUALIFICATION_D2_CANDIDATE_10_REPAIR.md;
- workplans/active/MLFF_TRAIN2_CUEQ_PARITY_REQUALIFICATION_D2_INDEPENDENT_REVIEW_HANDOFF_C10.md; and
- workplans/active/MLFF_TRAIN2_CUEQ_PARITY_REQUALIFICATION_WORKPLAN.md.

It does not modify the Candidate-10 semantic file and was not substituted for C10 as the Review target.

The earlier authoring commit bd49e53a9b066a7d5a8f29cb45fdabfdd152e8f7 is historical prefreeze state only.

The repository tree at the lifecycle basis contains no Candidate-10 Stage-C artifact. The C9-to-C10 lineage adds Review/repair/lifecycle material and Candidate 10 but no Candidate-9 or Candidate-10 Stage-C result. No Candidate outcome was used to choose Candidate-10 transport semantics, risk values, score definitions, source relation, or renewal law.

The accepted parent was independently reconstructed from:

- accepted D1/D2 kernel a759e81aa1b4c70c8fb513c569ddce57e99cbdb2;
- exact accepted D1/D2 source a4824d28775164aa942fd29fa97ee0957eb87e6f.

Historical Reviews R1-R6, repair records, Stage-A evidence, PEM entries FF-001/FF-002/SP-002/SP-003/SP-004, and pinned MACE 0.3.16 conversion sources were used as falsification/evidence context only, not as numerical authority.

**Overall disposition: PASS AS D2 CANDIDATE FAMILY FOR STAKEHOLDER INSTANCE RATIFICATION AND STAGE-C TESTING**

**SERIOUS CHALLENGE to accepted D1/D2 parent authority: NO**

Candidate 10 closes Review R6 at the correct D2 owner. The materiality-risk population is no longer the qualification launch-slot mixture. Every admissible qualification child must be generated under the exact production-equivalent role law, with the full conditional three-child law equal to $P_R^{\rm prod}\otimes P_R^{\rm prod}\otimes P_C^{\rm prod}$, and ordinary production CuEq must execute the same $\mathcal W_C$ contract. A slot/history/cache/concurrency regime that changes the candidate law is therefore inapplicable before any materiality aggregation.

No independent false-pass route or undefined authorizing relation remained after the required R1-R6 historical and numerical falsification sweep.

Candidate 10 remains immutable. This PASS does not ratify any concrete $\eta_{\rm NI}$, $q_{\rm cat}$, $n$, $E$, $\mathcal R_e$, $\mathcal Q$, renewal contract, or production law and does not authorize Stage C by itself.

## 2. Accepted-parent reconstruction and Challenge disposition

Accepted D2.DEF.060A remains source-closed: exact identities and decisions compare exactly; explicit accepted predicates use their exact accepted guards; other metric/equality relations must come from the exact accepted source; and a governed binary64 output with no accepted nonzero tolerance remains exact. Backend observations, performance, historical D4 thresholds, ULP counts, sample maxima, Huber scales, or convenience constants cannot mint a numerical-equivalence relation.

The accepted parent contains no CuEq-specific TRAIN2 equivalence relation. That absence is not a contradiction in the accepted parent: it means CuEq acceleration lacked accepted authority and required the present proposed D2 overlay. Candidate 10 does not retroactively promote Rev86, Stage-A evidence, current D4 behavior, or PEM into parent authority.

Accepted D1 delegates precision/equivalence and typed numerical-failure concretization to D2 while prohibiting threshold widening, role substitution, fabricated evidence, and materially changed execution semantics. Candidate 10 stays inside that delegation.

Therefore no SERIOUS CHALLENGE is raised against accepted D1/D2.

## 3. Production-equivalent child law

Candidate 10's $P_R^{\rm prod}$ and $P_C^{\rm prod}$ are not merely start-snapshot distributions. D2.CUEQ10.DEF.015 explicitly requires the whole arithmetic-relevant child execution law, including post-start stochastic/runtime evolution, to match the corresponding real production/reference role.

The applicability census explicitly reaches CUDA/driver/compiler caches, allocator/device residency, CUDA graphs, autotuning/generated-kernel caches, supervisor state, filesystem/page cache, clock/power/thermal regime, Torch/MACE/CuEq globals, persistent RNG streams, worker/thread/daemon state, analogous mutable owners, post-start interference, and production concurrency/resource-sharing semantics.

Every material owner must be one of:

1. reset/fixed to the production contract;
2. immutable in $\rho$;
3. independently regenerated under the production law; or
4. isolated so sibling execution cannot affect the child.

A start snapshot followed by qualification-only interference does not satisfy the candidate. A qualification child may overlap another only when the exact production concurrency/resource-sharing law is reproduced and proved. A fixed prewarm is admissible only when prospectively bound and reproduced in the production law. Failure to establish the law fails closed.

This is a coherent D2 applicability contract. It states the numerical/stochastic condition D3/D4 must realize without prescribing a scheduler, cache-clearing implementation, process supervisor, or storage mechanism.

## 4. Full joint-law falsification

For every admissible pre-assignment state $\lambda$ and assignment $a$, Candidate 10 requires:

$$
\mathcal L(\Omega^{R1},\Omega^{R2},\Omega^C\mid\Lambda=\lambda,A=a)
=
P_R^{\rm prod}\otimes P_R^{\rm prod}\otimes P_C^{\rm prod}.
$$

The following superficially correct-marginal constructions all fail that identity:

- a hidden common random variable shared by two children;
- a daemon/global RNG that correlates execution noise;
- anti-correlated $R1/C$ cache outcomes;
- a third child whose runtime state depends on a latent variable created by the first child;
- a pair coupling that changes $P(C^C=1)$ while retaining the correct individual marginals;
- sibling residue that affects only post-start arithmetic.

Thus marginal matching cannot authorize a correlated qualification.

The full three-child product requirement is stronger than the minimum needed separately for $q_R^\ast$, $q_C^\ast$, and the pre-assignment systematic-comparison theorem because, in particular, $R2$-$C$ independence is not independently required by the two pairwise materiality propositions. The excess strength is conservative: it can reject a key that a weaker valid design might have qualified, but it cannot create a false authorization. Candidate 10 explicitly fails closed rather than deriving a looser estimator from a non-product law. This strength is therefore not a blocking D2 defect.

## 5. Review-R6 slot-mixture adversary

For the required construction:

$$
q_{\rm cat}=0.08,
\qquad
(q_{C,1},q_{C,2},q_{C,3})=(0.20,0,0),
$$

the qualification-only uniform mixture is:

$$
q_{\rm mix}=\frac{0.20}{3}=0.066\overline6<0.08,
$$

while the high-risk first-launch production regime has:

$$
q_{\rm prod}=0.20>0.08.
$$

Candidate 10 rejects the construction before inference. The candidate child cannot simultaneously have the same $P_C^{\rm prod}$ law in all three qualification slots when its materiality probability is $0.20,0,0$ by slot.

The strengthened variants are also rejected:

- identical child start snapshots followed by divergent post-start cache behavior violate the whole-execution-law clause;
- a first/second-slot effect arising only after arithmetic begins violates post-start transport;
- a candidate law changed by prior reference execution violates prior-sibling invariance;
- production skipping or changing $\mathcal W_C$ makes the record inapplicable;
- production warm versus qualification cold, or vice versa, changes $P_C^{\rm prod}$;
- serialized qualification versus materially different concurrent production changes the law;
- qualification-only or production-only daemon sharing changes the law.

The same logic is required independently for the completed-state projection evaluator.

## 6. R5 pre-assignment theorem

Let $\tau$ swap $R2$ and $C$ while leaving $R1$ fixed. Candidate 10 retains only the pre-assignment label-exchange theorem. It does not require or rely on exchangeability after conditioning on a realized assignment.

For the deterministic launch-position nuisance $h=(0,1,2)$ and pair score equal to absolute position difference, the six assignments give strict-worse indicators:

$$(1,0,0,0,0,1)$$

up to permutation ordering, hence $P(B=1)=1/3$.

The shared $R1$ anchor does not require $R2$ and $C$ to be mutually independent for the R5 symmetry argument. Under Candidate 10's stronger product-law applicability contract, an actual material slot nuisance is separately excluded; retaining the R5 theorem is conservative regression protection and does not contradict the transport contract.

$\eta_{\rm NI}$ remains a randomized qualification-comparison coordinate only. It does not transport catastrophic production risk.

## 7. Materiality-risk population and conditioning

Under D2.CUEQ10.DEF.015, valid observations are draws from:

$$
P_R^{\rm prod}\otimes P_R^{\rm prod}
$$

for reference self-comparison and:

$$
P_R^{\rm prod}\otimes P_C^{\rm prod}
$$

for reference/candidate comparison.

Using the same $R1$ realization as the anchor makes $C^R$ and $C^C$ dependent within a triplet, but does not alter either required pair marginal. Candidate 10 does not claim those two event families are mutually independent.

The protected candidate proposition is explicitly pairwise disagreement risk against an independently fresh accepted-reference realization. It is not presented as an absolute probability that the candidate is scientifically wrong independent of reference variability.

Any production-start variable that materially changes the launch law must be bound in $\rho$, generated by the exact production mechanism, or otherwise represented in $P_C^{\rm prod}$. A qualification-only mixture cannot stand in for a different production law.

One source-defined materiality violation remains one event. Consumer count cannot average severity away.

## 8. Binomial and confidence model

With i.i.d. triplet pre-assignment states, exact per-child production-equivalent laws, independent triplets, prospective assignment, and fixed method coordinates, each systematic event $B_{p,j}$ is i.i.d. Bernoulli and each materiality-event sequence is i.i.d. Bernoulli under its corresponding production-equivalent pair law.

The simultaneous target is:

$$
\kappa=0.95,\qquad
\alpha=\frac{1-0.95}{4}=0.0125.
$$

The four one-sided statements are:

1. $p_{\max}$ systematic-shift statement for $S_{\max}$;
2. $p_{\Sigma}$ systematic-shift statement for $S_\Sigma$;
3. reference materiality risk $q_R^\ast$;
4. candidate materiality risk $q_C^\ast$.

For zero materiality events in $n$ trials, the exact one-sided Clopper-Pearson upper bound is:

$$
Q=1-\alpha^{1/n}.
$$

Therefore:

$$
n\ge
\left\lceil
\frac{\log\alpha}{\log(1-q_{\rm cat})}
\right\rceil
$$

is the correct finite-$n$ requirement for $Q\le q_{\rm cat}$.

Boundary challenges:

- $q_{\rm cat}=0$ has no finite zero-event certification and therefore no finite executable instance under this formula;
- as $q_{\rm cat}\to0.10^{-}$ the required finite $n$ remains well-defined;
- $\eta_{\rm NI}=0$ is valid but intentionally very conservative at the exact $p=1/2$ boundary;
- $\eta_{\rm NI}=q_{\rm cat}$ is permitted only as the stakeholder coupling, not as an event-family identity;
- tied/discrete score laws remain valid because the strict-worse probability under exchangeability is at most $1/2$;
- all-worse observations yield an upper bound incompatible with $1/2+\eta_{\rm NI}<0.60$;
- exact Clopper-Pearson behavior, not a normal approximation, governs near-boundary decisions.

Bonferroni simultaneous coverage does not require independence among the four statements. Finite confidence is not described as proof that the failure probability is zero.

## 9. Retry, failure, selection, and currentness attacks

Candidate 10 supplies no favorable-retry route.

Prospective design entropy is fixed before protected output. Startup failure, OOM, timeout, crash, unsupported runtime, nonfinite execution, failure to establish the production-equivalent boundary, or post-output infrastructure failure cannot be silently deleted, redrawn, reassigned, or coded as a Bernoulli zero.

The following are forbidden:

- assignment-seed search;
- repeated randomization until pass;
- deletion/replacement of failed triplets or children;
- adaptive warm-up until favorable;
- stopping after unfavorable initialization;
- replacing an unfavorable infrastructure realization;
- favorable-completion-only pooling;
- numerical-to-infrastructure failure relabeling;
- changing $n$ after observing results.

Realized assignment sequences are evidence-realization identity, not new semantic keys. A new sequence cannot reset a failed same-key qualification. A multi-attempt aggregation rule would be a new D2 method.

## 10. $q_{\rm cat}$ and $\eta_{\rm NI}$ semantics

Candidate 10 explicitly states:

$$
0\le\eta_{\rm NI}\le q_{\rm cat}<0.10
$$

as a stakeholder admissibility restriction, not a theorem relating the underlying event families.

The strict $0.10$ ceiling excludes the known R3 $10\%$ material-tail construction at the population level; it does not claim that $0.099$ is scientifically desirable. Finite testing can still make a confidence error at the predeclared rate.

A candidate can be strictly worse in ULP score more often than $1/2$ while remaining inside every source-owned materiality relation. Candidate 10 treats that as a separately ratified numerical-noninferiority risk coordinate. Because materiality and exact decisions are independently protected, this is a stakeholder risk choice rather than an unprotected scientific consequence.

## 11. Source-owned materiality relations

Candidate 10 does not reuse Rev86, current D4 behavior, ULP count, Huber scale, sample extrema, Stage-A observations, or Candidate observations as materiality authority.

The accepted parent resolves the current role-effective source relations through:

- D2.DEF.057 shared checkpoint/replay/integrity constraints;
- D2.DEF.058 exact role checkpoint threshold semantics with inclusive binary64 boundary;
- D2.DEF.059 exact CV outer threshold semantics;
- exact accepted practical-equivalence/ranking owners where applicable;
- exact tie, lexicographic, and accepted guard semantics;
- exact equality when no accepted nonzero relation exists.

D2.CUEQ10.DEF.018 adds strict distance-to-boundary/order-gap guards as sufficient fail-closed qualification conditions. They do not widen an accepted source relation or create a false pass: the exact accepted classification/decision must also agree, exact ties remain exact, and missing source authority still makes the role unqualifiable. The extra guards can only reject an otherwise source-compatible realization. Their conservative strength therefore does not mint a looser numerical tolerance.

No governed role may be qualified merely because an observed Candidate discrepancy appears small.

## 12. Complete consumer closure

Candidate 10 binds exact $E$ and protects the actual role-effective chain:

TRAIN2 -> checkpoint/monitor -> retention/admissibility -> completed-state projection -> EVAL2/publication.

The final completed portable state is always evaluated after the last optimizer/EMA mutation. Therefore the historical final-hidden-coordinate adversary is closed.

Latent optimizer state need not be cross-basis equal. A latent difference that later affects a governed consumer inside $H$ is exposed by the complete horizon; a latent difference that never affects $H$ or exact $E$ is outside the bounded claim.

Future consumers outside exact $E$ remain outside authority. Held-out $E$ labels/metrics are observation-only and cannot feed training, membership, checkpoint choice, early stopping, or another upstream decision.

## 13. IEEE ULP primitive

For each exact binary dtype, Candidate 10 defines rank from the signless IEEE magnitude bits with signed zero collapsed.

The required local identities hold:

$$
r(-{\rm minsub})=-1,\qquad
r(-0)=r(+0)=0,\qquad
r(+{\rm minsub})=1.
$$

Positive and negative normal ordering, subnormal/normal boundaries, exponent transitions, and largest finite values are monotone under this rank. NaN and infinity fail. Cross-dtype comparison is undefined unless another accepted conversion relation exists.

Binary64 control quantities therefore cannot be silently coerced to binary32.

## 14. $S_{\max}$ and exact $S_\Sigma$

Candidate 10 defines:

$$
S_{\max}=\max_i z_i,
\qquad
S_\Sigma=\sum_i z_i
$$

with exact integer accumulation over a canonical atomic consumer trace.

Zero-coordinate padding cannot dilute either score. Tensor splitting/merging or serialization-block changes do not alter the canonical atomic trace. Reordered integer accumulation cannot change the exact sum. $S_{\max}$ catches rare large displacement; $S_\Sigma$ catches broad low-level displacement.

Different geometries can share the same score pair. That does not create scientific equivalence because source-owned $\mathcal R_e$ and exact discrete decisions are independent gates. The score pair never becomes a universal physical tolerance.

## 15. Exact scientific decisions

Candidate 10 requires exact agreement on every governed discrete consequence, including checkpoint event/admissibility, retention classification, selected checkpoint, representative/final ordering, exact ties, outer acceptance, publication/deployment selection, and other protected decisions.

A stochastic score pass cannot rescue a discrete mismatch.

## 16. Complete portable-state inventory

Pinned MACE 0.3.16 confirms that state_dict alone is not a complete portable-state oracle. In particular, the upstream e3nn/CuEq conversion code manually transfers interaction avg_num_neighbors outside ordinary state_dict mapping, and model reconstruction also depends on heads, atomic energies, scale/shift, cutoff/radial/interaction configuration, architectural flags, and other constructor attributes.

Candidate 10 explicitly requires parameters, buffers, heads/order, elemental references, scale/shift/normalization, cutoff/architecture state, interaction attributes including avg_num_neighbors, forward-affecting ordinary Python attributes, flags/config, and every additional forward-capable state item proven by the pinned dependency.

Omission, duplication, wrong correspondence, wrong permutation, wrong k-range, or fallback injection is structural failure.

## 17. Projection-oracle independence and exact semantic transform

Pinned MACE 0.3.16 directly confirms the relevant production semantic owners:

- convert_e3nn_cueq.py defines get_kmax_pairs and calls symmetric_contraction_proj;
- cg_cueq_tools.py constructs the reduced-CG projection using production representation machinery and numpy.linalg.pinv before rationalization;
- convert_cueq_e3nn.py calls symmetric_contraction_proj and then numpy.linalg.pinv for reverse mapping;
- both conversion directions transfer avg_num_neighbors outside ordinary state_dict mapping.

Candidate 10 correctly forbids the independent oracle from reusing the production converter/inverse converter, generated mapping tables, production key enumeration, get_kmax_pairs, symmetric_contraction_proj, or production projection/pseudoinverse matrices.

For a nontrivial transform, $A^\ast$ is owned by the accepted representation mathematics and must be independently certified. Existence and uniqueness are required on the active authenticated subspace. A rank-deficient/non-unique inverse without an accepted canonical representative or an ill-conditioned transform that prevents correct-rounding certification fails closed.

This closes the prior common-owner and arbitrary-pseudoinverse false-pass routes.

## 18. Production coefficient qualification and floating transfer

Candidate 10 requires:

$$
\widehat A={\rm RN}_d(A^\ast)
$$

bitwise in the exact production coefficient dtype before output agreement is considered.

Therefore the scalar adversary:

$$
A^\ast=1,\qquad \widehat A=100,\qquad x=1
$$

fails at coefficient qualification and cannot self-authorize through evaluator agreement.

Wrong sign, permutation, k-range, subtle coefficient perturbation, or a matrix that happens to agree on one evaluator sample likewise fails when it is not the independently certified correctly rounded matrix.

The floating transfer bound:

$$
\gamma_n=\frac{nu}{1-nu}
$$

is applicable only after the actual realized reduction graph is certified: operation count, reduction order, fused operations, accumulation/coefficient dtype, round-to-nearest mode, subnormal/flush-to-zero behavior, underflow/overflow, cancellation, conditioning, and kernel identity are bound. Nominal tensor length cannot substitute for the realized arithmetic graph. An unproved assumption fails closed rather than widening the bound.

## 19. Projection evaluator transport

The projection evaluator has its own exact production/evaluation-equivalent child laws and qualification design. The R6 slot-risk adversary $(0.20,0,0)$ is rejected for the evaluator for the same structural reason as TRAIN2: a qualification-slot mixture cannot authorize a different fixed evaluator law.

Structural inventory, correspondence, transform, or coefficient failure remains terminal. Evaluator output agreement cannot rescue a structural failure.

After valid projection, EVAL2 numerical forward provider identity is e3nn. CuEq remains provenance of completed TRAIN2 state only.

## 20. Loader, TRAIN2, restart, source/DATA6, dtype, and doctor scope

Candidate 10 preserves accepted current P5 exposure:

- multihead replay/pt_head corpus first;
- target corpus second;
- shuffle after combined index layout;
- native accepted sampler;
- no balancing sampler;
- no intentional target duplication;
- drop_last=true;
- target-only foundation adaptation where applicable;
- complete accepted optimizer horizon.

Distributed execution is outside applicability until separately accepted.

Restart is same-backend only from an exact authenticated boundary. No e3nn-to-CuEq or CuEq-to-e3nn TRAIN2 continuation is inferred. Completed-state CuEq-to-e3nn projection is post-TRAIN2 only.

Source/DATA6 remains e3nn absent another accepted D2 relation. Candidate-10 TRAIN2 evidence cannot authorize source inference, pseudolabel generation, descriptor/FPS selection, or DATA6 CuEq. Binary32 is the only proposed CuEq TRAIN2 member; FP64 CuEq TRAIN2 remains unsupported, and forward-only FP64 evidence cannot authorize training.

Routine doctor may authenticate a current qualification and perform cheap finite reachability only. It cannot estimate risk, change $n$, widen tolerances, create $\mathcal R_e$, authorize another key, authorize source/DATA6 or FP64 TRAIN2, switch backend mid-run, or revive stale evidence.

## 21. Historical regression R1-R6

The historical blockers remain closed:

| Historical Review | Required closure | Candidate-10 result |
|---|---|---|
| R1 / C4 | full TRAIN2 consequence; no borrowed forward tolerance; correct experimental unit; no Huber-as-parity; difficult-regime coverage; independent state transfer; role/source closure | CLOSED by complete horizon/E, source-owned materiality, process-level stochastic law, no observation-minted tolerance, and independent projection hierarchy |
| R2 / C5 | no delta-sqrt-u scale; no finite optimizer/EMA probe extrapolation; no recurrence/secant; no sparse anchors; no metadata-only windowing; finite-sample semantics; no sample-max tolerance; source/DATA6 narrowing | CLOSED |
| R3 / C6 | final hidden coordinate; systematic-shift false pass; signed-zero ULP; converter/oracle common owner | CLOSED |
| R4 / C7 | ungrounded pmax=0.60; invalid fixed-cycle Binomial model; candidate/reference material-tail protection; dimensional dilution; self-authorizing projection coefficients/A-star ambiguity | CLOSED |
| R5 / C8 | conditional exchangeability after realized assignment | CLOSED by pre-assignment label-exchange theorem and prospective uniform assignment |
| R6 / C9 | qualification slot-mixture risk does not transport to actual production start | CLOSED by exact per-child production-equivalent full joint law and exact production use of $\mathcal W_C$ |

No old Rev86 tolerance reuse, finite action window, recurrence/secant extrapolation, metadata-only difficult-regime sampling, optimizer-state equality demand, EMA omission, final-step omission, source/DATA6 widening, FP64 promotion, signed-zero defect, score dilution, projection common-owner leakage, coefficient self-authorization, non-unique inverse acceptance, R5 conditional-exchangeability defect, or R6 population-transport defect was reintroduced.

## 22. PEM applicability

The applicable Project Engineering Memory entries were treated only as evidence/context:

- FF-001: directly relevant to portable MACE construction/inventory and duplicated-owner drift;
- FF-002: directly relevant to authenticated same-backend restart boundaries;
- SP-002: supports fail-closed identity/currentness authentication;
- SP-003: supports immutable/content-addressed lifecycle boundaries;
- SP-004: supports qualification through real owners and target-host production regimes.

None mints D2 tolerance, risk, stochastic law, or projection semantics.

## 23. D2/D3/D4 authority boundary

Candidate 10 remains D2.

It specifies numerical/stochastic conditions that an implementation must satisfy, including exact execution-law transport, independence/isolation, currentness, and failure semantics. It does not prescribe scheduler decomposition, persistence architecture, package layout, storage mechanism, process-supervisor implementation, or a particular concurrency mechanism.

Where implementation choices cannot realize the required law, the candidate fails closed rather than adding compensating D2 machinery.

## 24. Required falsification-program matrix

| # | Required falsification area | Result |
|---:|---|---|
| 1 | immutable identity/lifecycle/no Stage C | PASS |
| 2 | accepted-parent reconstruction | PASS; no parent challenge |
| 3 | whole production-equivalent child law | PASS |
| 4 | full joint-law adversaries | PASS; incorrect marginals-only couplings rejected |
| 5 | R6 slot-mixture adversary | PASS |
| 6 | R5 pre-assignment theorem | PASS |
| 7 | materiality-risk population | PASS |
| 8 | Binomial/Clopper-Pearson/Bonferroni | PASS |
| 9 | retry/failure/selection attacks | PASS |
| 10 | $q_{\rm cat}$ / $\eta_{\rm NI}$ semantics | PASS |
| 11 | source-owned materiality relations | PASS/fail-closed when absent |
| 12 | complete consumer closure | PASS |
| 13 | IEEE ULP primitive | PASS |
| 14 | $S_{\max}$ / exact $S_\Sigma$ | PASS |
| 15 | exact scientific decisions | PASS |
| 16 | complete portable-state inventory | PASS |
| 17 | projection-oracle independence | PASS |
| 18 | exact semantic transform $A^\ast$ | PASS/fail-closed on non-uniqueness |
| 19 | production coefficient qualification | PASS |
| 20 | floating transfer error | PASS subject to realized arithmetic proof; otherwise fail closed |
| 21 | projection evaluator transport | PASS |
| 22 | loader/TRAIN2 semantics | PASS |
| 23 | restart scope | PASS |
| 24 | source/DATA6 and dtype scope | PASS |
| 25 | EVAL2 identity | PASS |
| 26 | routine doctor scope | PASS |
| 27 | historical R1-R6 regression | PASS |
| 28 | authority/architecture boundary | PASS |

## 25. Lifecycle consequence and next gate

Candidate 10 is now a Review-passed immutable D2 candidate family, not accepted project authority.

The next gate is stakeholder ratification of one exact reviewed Candidate-10 method instance. Before any Stage-C execution, that ratification must bind at least:

- exact $\eta_{\rm NI}$;
- exact $q_{\rm cat}$;
- exact $n$ and any evaluator $n_{\rm eval}$;
- exact $E$;
- every exact source-owned $\mathcal R_e$;
- exact $\mathcal Q$ and evaluator design;
- exact $\mathcal W_{\rm pre}$, $\mathcal W_R$, and $\mathcal W_C$;
- exact $P_R^{\rm prod}$ and $P_C^{\rm prod}$;
- exact projection/evaluator production laws and oracle/transform identities.

Only after that human ratification may fresh Candidate-10 Stage-C evidence run.

D3/D4 remain blocked until Stage C passes and the later lifecycle contract authorizes handoff.

## 26. Final disposition

**PASS AS D2 CANDIDATE FAMILY FOR STAKEHOLDER INSTANCE RATIFICATION AND STAGE-C TESTING**

**SERIOUS CHALLENGE to accepted D1/D2 parent authority: NO**
