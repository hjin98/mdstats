# Active workplans

Active workplans are temporary engineering coordination and do not define current mdstats behavior by themselves.

There is one active MLFF implementation workplan:

- `MLFF_TARGET_SIZE_TRAINING_PRIORITY_EVALUATION_LADDER_ARCH_RESET_WORKPLAN.md` — V5 freeze candidate after an implementation dry-run against current source. It defines one target-size study with exactly one resolved target label compatibility identity; pre-target DATA5 without CV; correlation-safe `P_train/P_eval`; distinct role-independent base evidence, P_train selection preparation, and exact-membership gradient-bearing training preparation; one `pi_train`; one frozen nested `M1/M2/M3`; exact n1/n2/n3 continuation; `Nmax + M3` nominal capacity; exact selected-data freeze; post-selection exact-membership correlation-safe validation-only CV; fresh exact-`T_selected` production; M3 target-side checkpoint control; replay admissibility-only; outer target validation held out; destructive retirement of fixed/complement/domain-prefix/CV-coupled legacy state; and preservation of shared TRAIN2/OPT-EVAL4 resource ownership.

All previously active plans/amendments were retired to `../archive/retired-2026-08-28/`; archived material is historical only and does not independently impose active gates.

Full long real-data/GPU production qualification remains separate and deferred to final release. Scientific M-ladder sampling qualification is separately reported and may be `deferred/unavailable` when representative evidence is unavailable; it cannot be called passed without evidence.
