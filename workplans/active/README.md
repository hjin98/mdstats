# Active workplans

Active workplans are temporary engineering coordination and do not define current mdstats behavior by themselves.

The current MLFF implementation authority is:

- `workplans/active/MLFF_TARGET_SIZE_INTEGRATION_CLOSURE_REPAIR_WORKPLAN.md`

Independent Software Design review round 2 of branch candidate `bae8e8cc7896286d5342a7421619383096b9dc0c` / executable `3935c9d0f19d309cbe1191792884680d68336c62` is **NO-PASS**. The accepted scientific architecture remains intact, and review accepts the narrow manual prepared/P2 dependency, the pure committed-state selection-view projection, the real auto->manual poison-P3 acceptance, the aliased scalar-state AST guard, and the current-side `frozen_entries` compatibility reopener.

Remaining blocking closure work is deliberately narrow:

1. restore the exact frozen P5A6 compatibility baseline (`1670275487d29bbcde4c59efafdef9d1f8b0ced7`, tree `17e2c5609974712bda1efd3375f09f42da830f68`) rather than substituting the later scalar commit `fc69a3d...`; if the exact Git objects cannot be recovered, keep that compatibility claim blocking/unavailable and return only that authority surface to Design;
2. finish semantic reconciliation of current normative architecture/specification sources that still claim a campaign-global scalar `T_selected` or “one protocol-global target-size decision with one exact global selected membership”;
3. re-derive and execute the complete final affected regression, including existing result-view/currentness consumers omitted from the recorded run, and record a resolvable final candidate/executable identity.

Repair policy remains alteration/removal/current-owner rewiring, not additive wrappers, migrations, caches, duplicate state, compatibility machinery, or a new view authority.

Previous completed workplans remain archived as historical evidence and are not current implementation authority.

Full long-running real-data/GPU/CuEq/LAMMPS production qualification remains separate and deferred to the established final-release/user-machine qualification stage.
