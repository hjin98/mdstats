# Active workplans

Active workplans are temporary engineering coordination and do not define current mdstats behavior by themselves.

The current MLFF implementation authority is:

- `workplans/active/MLFF_TARGET_SIZE_INTEGRATION_CLOSURE_REPAIR_WORKPLAN.md`

Independent Software Design review round 3 of candidate `c9dfe2bc7a0176f647439b6af9f80c6ead49cecb` / substantive source-test head `23854d72555014c6d4e834d85a5ad292b228f93d` is **NO-PASS**, narrowly.

Round 3 accepts the previously repaired runtime architecture and closes the affected-regression/candidate-identity blocker: the narrow manual prepared/P2 dependency, pure committed-state selection view, real auto->manual poisoned-P3 acceptance, aliased scalar-state structural guard, current `frozen_entries` normalization, and the expanded result-view/currentness regression all pass review.

Two blockers remain:

1. **P5A6 compatibility evidence unavailable.** Exact provenance pins were restored and no substitute producer/fixture was used, but the historical P5A6 Git object is unreachable and the original preserved `qualification/p6-p5a6-compat/workspace/` is absent. Recover either the exact historical source and run the existing driver, or recover the original workspace bytes and authenticate them against the committed fixture identity/content/database snapshots before exercising current real owners. Do not synthesize, migrate, or substitute the fixture. If neither artifact is recoverable, the supported-history decision remains open in Design.
2. **Part VI normative documentation still stale.** `docs/arch_manuals/mlff_training_data/60_execution_performance.md` still describes the paired-seed screen as flowing to a selected binding / “one selected size or typed scientific failure.” Reconcile it to advisory recommendation/no-recommendation -> operator-owned ordered provisional collection -> `cross-validate` atomic freeze -> per-size CV/production, strengthen the existing doc assertions, and regenerate derivatives.

If the next repair is documentation/tests plus historical-artifact recovery only, the Round-3 200-pass product-runtime affected suite remains reusable. Any runtime/state/currentness/compatibility-reader change requires fresh affected regression.

Repair policy remains alteration/removal/current-owner correction, not additive wrappers, migrations, caches, duplicate state, synthetic fixtures, or new scanners.

Previous completed workplans remain archived as historical evidence and are not current implementation authority.

Full long-running real-data/GPU/CuEq/LAMMPS production qualification remains separate and deferred to the established final-release/user-machine qualification stage.
