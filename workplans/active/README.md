# Active workplans

Active workplans are temporary engineering coordination and do not define current mdstats behavior by themselves.

There is currently one active MLFF implementation workplan:

- `workplans/active/MLFF_DOWNSTREAM_INTEGRATION_CLOSURE_WORKPLAN.md`

Its current binding implementation-review amendment is:

- `workplans/active/MLFF_DOWNSTREAM_INTEGRATION_CLOSURE_REVIEW_REOPEN.md`

The stakeholder explicitly adopted **Scientific Software Development Protocol 6.0.0** for review and closeout from 2026-09-09 forward. The binding amendment records the migration from the parent plan's historical Protocol 5.16 control-plane binding while preserving all non-conflicting task-specific scientific/product/D3 authority.

Protocol 6 Software Design review of executable candidate `13859556cd4d472837a9e171c2be32e59d5e6d82` found the previous source-level recovery and dependency-facing foundation-path blockers closed. In particular, durable TRAIN2 continuation is now jointly reconciled with completed authenticated P5 materialization and canonical MACE execution evidence; foreign sibling continuation with an equal runtime-plan shape is rejected before EVAL2/publication; and all three configured foundation path forms reach real `MacePostSelectionTrainer` dependency projection.

The overall review remains **NO-PASS / REOPENED only for executable acceptance evidence**. GitHub exposes no final check runs/status evidence for `13859556...`, and the review host cannot execute the repository locally. The next action is therefore evidence-only: run the affected pytest/static/integration surface named in the binding amendment on that exact executable candidate. Do not change product code unless those executions expose a genuine failure.

Most earlier downstream repairs remain accepted and must be preserved: canonical configured-path semantics, foundation locator/content-identity separation, path-free replay source/split lineage, complete multi-size CV verdicts, collection-wide final-production admission, P5 publication ownership/P7 consumption, P7 reference-root canonicalization, recovery preservation of corrupt/foreign state, and the reconciled architecture documentation.

Implementation/review coordination continues on `fix/mlff-downstream-integration-closure`. Full long-running real-data/GPU/CuEq/LAMMPS production qualification remains deferred until the final release qualification package.

The narrow single-source replay-lineage adapter repair is implemented and retired to:

- `workplans/archive/MLFF_P5_SINGLE_SOURCE_REPLAY_LINEAGE_ADAPTER_REPAIR_WORKPLAN.md`

The most recently closed target-size integration plan before these repairs is:

- `workplans/archive/MLFF_TARGET_SIZE_INTEGRATION_CLOSURE_REPAIR_WORKPLAN.md`

After independent Software Design Review passes with the required executable evidence, retire/archive the completed active plan/amendment according to repository policy and reconcile this index as lifecycle closeout.
