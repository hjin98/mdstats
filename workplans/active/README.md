# Active workplans

Active workplans are temporary engineering coordination contracts. They do not define current mdstats product behavior by repository presence alone; current behavior remains owned by accepted D1-D4 authority and conforming implementation.

## Current active MLFF work

`MLFF_POST_SELECTION_UNIVERSAL_LOSS_MONITOR_AND_CV_METHOD_RESTORATION_WORKPLAN.md` remains active on `fix/mlff-post-selection-method-restoration`.

The earlier P5 D1/D2 Serious Challenge has been adjudicated and resolved. The accepted restored foundation-P5 method uses pinned MACE native `UniversalLoss` with `huber_delta = 0.01` and global E:F:S coefficients 1:10:1, selected-head foundation-residual E0 fitting with composition-transfer checks, TRUE_DFT as the canonical replay-label default, one protected campaign-common exact 256-frame target checkpoint monitor over neutral `OUTER_MONITOR`, and default three-fold post-selection CV. P3 target-size screening and P5 scratch retain their separately accepted methods.

Current D3 preserves one P5 method authority, one common-monitor owner, the existing atomic-reference fitter, and an acyclic `role plan -> fitted preparation -> materialization/run evidence` lineage. Target-size M3 remains P3 evidence and is not a P5 checkpoint, final-plan, final-seed-ranking, publication, or currentness ancestor.

Independent Integration & Verification Review R4 is recorded at `audits/MLFF_POST_SELECTION_RESTORATION_IMPLEMENTATION_REVIEW_R4_2026-09-14.md`. It reviews executable/spec/test candidate `8df2dd798c3390071b89d54fbd9e5d88c8b8e4cb`; assembled branch head `bf98b47ad39831364abef0a53957f421df8b4d4b` adds only regenerated documentation PDFs. R4 closes the G1B one-way plan/preparation ancestry repair and G12A replay-transport/currentness/integration repair. No D1/D2/D3 Serious Challenge or remaining D4 implementation/integration blocker was found.

The repaired replay boundary now guarantees neutral `config_weight=1.0` plus exact binary energy/force/stress availability masks for every current foundation-P5 replay transport, rejects noncanonical directly consumed legacy replay files, invalidates only stale derived replay views, preserves unchanged expensive replay source/split/prediction/qualification parents, and leaves authenticated P5 restart intact. Current guide/config/DATA9B3/P7/DATA8 prose is reconciled with the accepted runtime semantics.

The workplan remains active **only because G13 and final closeout are incomplete**: a current-candidate bounded real-LTA scientific pilot and required three-fold affected qualification remain required. This is a scientific qualification/acceptance blocker, not authorization for another compensating D4 mechanism. If correctly restored TRUE_DFT replay still shows material forgetting comparable to the prior failure regime, reopen D1/D2 rather than adding a patch layer.

Production-scale GPU/CuEq/LAMMPS/MLIAP qualification remains deferred to the final complete release package and final user-side machine test.

There is no active target-order redesign. The withdrawn FPS/coverage Gate-A lineage remains archived and does not alter the accepted target-size method.

Completed/superseded workplans belong under `workplans/archive/`.
