# Active workplans

Active workplans are temporary engineering coordination contracts. They do not define current mdstats product behavior by repository presence alone; current behavior remains owned by accepted D1-D4 authority and conforming implementation.

## Current active MLFF work

`MLFF_POST_SELECTION_UNIVERSAL_LOSS_MONITOR_AND_CV_METHOD_RESTORATION_WORKPLAN.md` remains active on `fix/mlff-post-selection-method-restoration`.

The earlier P5 D1/D2 Serious Challenge has been adjudicated and resolved. The repaired D1/D2 method is accepted on this branch: foundation adaptation uses pinned MACE native `UniversalLoss` with `huber_delta = 0.01` and global E:F:S coefficients 1:10:1, selected-head foundation-residual E0 fitting with composition-transfer checks, TRUE_DFT as the canonical replay-label default, one protected campaign-common exact 256-frame target checkpoint monitor over neutral `OUTER_MONITOR`, and a default three-fold post-selection CV policy. P3 target-size screening and P5 scratch retain their separately accepted methods.

Current D3 preserves one P5 method authority, one common-monitor owner, the existing atomic-reference fitter, and an acyclic `role plan -> fitted preparation -> materialization/run evidence` lineage. M3 remains P3 evidence and is not a P5 checkpoint, final-plan, final-seed-ranking, or publication ancestor.

The implementation restoration is assembled. Independent Review R2 at `audits/MLFF_POST_SELECTION_RESTORATION_IMPLEMENTATION_REVIEW_R2_2026-09-14.md` closes the narrow D4 ancestry contradiction and accepts the candidate-bound G11/G12 evidence for committed candidate `cd0c3a16d08f218ac31030d061178bf25d2aed25`.

The workplan remains active only because G13 and final closeout are not complete: a current-candidate bounded real-LTA scientific pilot and required three-fold affected qualification are still required before the implementation can receive final Protocol 6.3 PASS. This is a qualification/acceptance blocker, not authorization for another compensating D4 mechanism. If correctly restored TRUE_DFT replay still shows material forgetting comparable to the prior failure regime, reopen D1/D2 instead of adding a patch layer.

Production-scale GPU/CuEq/LAMMPS/MLIAP qualification remains deferred to the final complete release package and final user-side machine test.

There is no active target-order redesign. The withdrawn FPS/coverage Gate-A lineage remains archived and does not alter the accepted target-size method.

Completed/superseded workplans belong under `workplans/archive/`.
